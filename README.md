# mira2

Build the project using Cabal:

```bash
cabal build
```

Run the executable:

```bash
cabal run mira2
```

Run the test suite:

```bash
cabal test
```

For verbose test output:

```bash
cabal test --test-show-details=always
```
